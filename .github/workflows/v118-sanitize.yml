name: FROMAGO v11.8 sanitation (completed)

on:
  workflow_dispatch:

jobs:
  completed:
    runs-on: ubuntu-latest
    steps:
      - run: echo "FROMAGO v11.8 sanitation completed; no automatic patching remains enabled."
